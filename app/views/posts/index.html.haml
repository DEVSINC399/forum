#posts
    - @posts.each do |post|
        .post
            %h2.title= link_to post.title, post
            %p.content= post.content
            %p.date
                Published
                = time_ago_in_words(post.created_at)
                ago